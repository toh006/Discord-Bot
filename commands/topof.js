const { prefix } = require('../config.json');
module.exports = {
	name: 'topof',
	descrption: 'Subscribes the bot to the top posts of a given subreddit',
	usage: '[subreddit]',
	execute(message, args){
	},
};
