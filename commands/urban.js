const { prefix } = require('../config.json');
module.exports = {
	name: 'urban',
	descrption: 'finds and returns a given urbandictionary page',
	usage: '[search term]',
	execute(message, args){
	},
};
